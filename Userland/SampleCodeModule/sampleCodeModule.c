/* sampleCodeModule.c */
#include "./include/shell.h"
int main() {
	run_shell();
	// int returned = 0x000CAC71;
return 0x000CAC71;
	// while(returned != 1){
	// 	printf(PROMPT, username);
	//char command[MAX_LEN + 1];
	//scanf("%[^\n]", command);
	//returned = get_cmd_input(command);
	// }
	//return 0x000CAC71;
}